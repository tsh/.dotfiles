#!/usr/bin/env bash
apt-get update > /dev/null
apt-get install git, zsh
# zsh as default shell
touch ~vagrant/.bash_profile
echo export SHELL=$(which zsh) >> ~vagrant/.bash_profile
echo exec /bin/zsh -l >> ~vagrant/.bash_profile
